# Conda Environments

This folder contains YAML files describing Conda environments for each tool used in the pipeline (e.g., geNomad, iPHoP, Taxmyphage).

To create a new environment:
```bash
conda env create -f genomad_env.yml
conda activate genomad_env
